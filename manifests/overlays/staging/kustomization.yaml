apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: staging-
labels:
  - includeSelectors: false
    pairs:
      environment.opsia.dev/name: staging
patches:
  - path: deployment-patch.yaml
