apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: dev-
labels:
  - includeSelectors: false
    pairs:
      environment.opsia.dev/name: dev
patches:
  - path: deployment-patch.yaml
