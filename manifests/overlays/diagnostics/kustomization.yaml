apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
components:
  - ../../components/diagnostics
namePrefix: diagnostics-
labels:
  - includeSelectors: false
    pairs:
      environment.opsia.dev/name: diagnostics
